// @ts-ignore
import { login } from '@/utils/casUtils';
import { describe, expect, it } from 'vitest';

describe('casUtils', () => {
  it('test 1 - ', () => {
    // TODO
    expect(true).toBe(true);
  });
});
